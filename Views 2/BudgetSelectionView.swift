import SwiftUI
import Observation

struct BudgetSelectionView: View {
    let budgetStore: BudgetStore
    @State private var showCreateBudget = false
    
    var body: some View {
        // ... rest of the code
    }
}